<nav
    class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme"
    id="layout-navbar">

    <div class="layout-menu-toggle navbar-nav align-items-xl-center me-3 me-xl-0 d-xl-none">
        <a class="nav-item nav-link px-0 me-xl-4" asp-controller="Account" asp-action="Profile">
            <i class="bx bx-menu bx-sm"></i>
        </a>
    </div>

    <div class="navbar-nav-right d-flex align-items-center" id="navbar-collapse">
        <ul class="navbar-nav flex-row align-items-center ms-auto">

            <li class="nav-item dropdown">
                <a class="nav-link d-flex align-items-center gap-2"
                   href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">

                    <span class="fw-semibold d-none d-md-inline">@ViewBag.UserEmail</span>

                    <div class="avatar">
                        <img src="@ViewBag.UserAvatar" alt="User Avatar" class="w-px-40 h-px-40 rounded-circle"/>
                    </div>
                </a>

                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                    <li>
                        <a class="dropdown-item" asp-controller="Account" asp-action="Profile">
                            <div class="d-flex align-items-center">
                                <div class="avatar me-3">
                                    <img src="@ViewBag.UserAvatar" alt="User Avatar" class="w-px-40 h-px-40 rounded-circle"/>
                                </div>
                                <div>
                                    <span class="fw-semibold d-block">@ViewBag.FirstName @ViewBag.LastName</span>
                                    <small class="text-muted">@ViewBag.BirthDate.ToString("dd.MM.yyyy")г.</small>
                                </div>
                            </div>
                        </a>
                    </li>
                    <li><div class="dropdown-divider"></div></li>
                    <li><a class="dropdown-item" asp-controller="Account" asp-action="Profile"><i class="bx bx-user me-2"></i> @DashboardText.MyProfile</a></li>
                    <li><a class="dropdown-item" asp-controller="Account" asp-action="NotificationPreferences"><i class="bx bx-cog me-2"></i> @DashboardText.Settings</a></li>
                    <li><a class="dropdown-item" asp-controller="Automation" asp-action="BillReminders"><i class="bx bx-credit-card me-2"></i>@DashboardText.Automation</a></li>
                    <li><div class="dropdown-divider"></div></li>
                    <li>
                        <form method="post" asp-controller="Authentication" asp-action="Logout">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="dropdown-item"><i class="bx bx-power-off me-2"></i>@AuthenticationText.Logout</button>
                        </form>
                    </li>
                </ul>
            </li>

        </ul>
    </div>
</nav>
